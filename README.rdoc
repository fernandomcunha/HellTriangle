== README

Here is how to build it:

  - Clone repository `git clone git@github.com:fernandomcunha/HellTriangle.git`

  - To run tests just call `rake`

  - To run the application just call `ruby hell_triangle.rb`
